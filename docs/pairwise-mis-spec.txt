{
  "summary": "Derive and adversarially verify pairwise-MIS weights for the twopointfive ReSTIR merges",
  "agentCount": 10,
  "logs": [],
  "result": {
    "spec": "FINAL SCHEME: confidence-budget pairwise MIS (canonical anchored in every pair), with albedo-less PROXY targets on BOTH sides of every MIS ratio and the TRUE canonical target reserved for resampling weights, stored targetPdf, and finalize. Backbone is the \"code\" derivation (survived both verdicts, verified against the shaders); pair budgets M_i/Cn come from the \"theory\" derivation (its DI machinery survived; the substitution of proxies into its ratios is covered by the verified \"partition holds for ANY positive q\" argument). Theory's two refuted GI errors are structurally avoided (see GI). All formulas below are per-pixel, per-frame.\n\n=== SOURCES / ADMISSION (DI and GI identical) ===\nCanonical c: the UNCHANGED fresh stream. DI: canon = generateReservoir(h.p,h.n,v,m,U.restirCandidates,fVis) then the OLD finalizeReservoir (W_c = wSum/(M_c*targetPdf)); M_c = canon.M (~8, includes invalid candidates — correct, they belong to the uniform technique whose constant 1/M is already the balance heuristic there). GI: the single fresh bounce as a 1-sample reservoir: giUpdate(samplePos,sampleNrm,rad,h.p, luminance(bounceWeight*rad), freshTarget, 1.0) then the OLD finalizeGIReservoir (W_c = min(wSum/tp,32)); M_c = 1.\nNeighbours i = temporal pixel pp (if U.restirTemporal) plus each spatial tap qp: ADMITTED iff the existing geometry gates pass — clip.w>0, uv in bounds, abs(pnd.w−h.t) < 0.12*max(h.t,1), dot(pnd.xyz,h.n) > 0.9 — and NOTHING else. Admission must never read prev.W, prev.wSum, or any target value. M_i = min(prev.M, U.restirMCap) temporal / min(prev.M, U.restirMCap*0.5) spatial, applied ONCE at load and used identically everywhere below. Cn = Σ M_i over admitted; k = count. If k==0 or Cn<=0: res = carry = canon (old finalize), bypass everything below — bit-identical to the current no-reuse path.\n\n=== DI ===\nReceivers: r_c = (h.p, h.n, fVis=flashTargetVis(h.p)); r_i = (x_i, n_i, fv_i) with n_i = pnd_i.xyz, x_i = prevWorldFromPixel(qp, dims, pnd_i.w) reconstructed via new uniforms prevInvViewProj+prevCamPos (pnd.w is Euclidean ray distance), fv_i = flashTargetVis(x_i).\nProxy (one fixed function per receiver, used at BOTH evaluation points): qT_r(idx, sp) = max(dot(n_r, normalize(sp−x_r)), 0) * luminance(radianceFromLight(idx, x_r, sp)) * select(1, fv_r, idx==0), d2 clamped at 1e-4. Radiance MUST be re-evaluated at x_r (spot-cone zeros are genuine support boundaries). The neighbour's STORED targetPdf never appears in any ratio. Zero-set equality with the true target is guaranteed by the 0.02 per-channel refl floor (common.wgsl:769) and the 0.08 flashVis floor (pathtrace.wgsl:94); the fv factor is optional for correctness (never zero), kept symmetric on both receivers for variance.\nTrue target: pC(idx, sp) = risTarget(m, h.n, dir, radianceFromLight(idx, h.p, sp)) * select(1, fVis, idx==0) — exactly today's merge tp.\nMIS weights, X_i = (prev_i.lightIdx, prev_i.samplePos), X_c = (canon.lightIdx, canon.samplePos), safeDiv(a,b) = b>1e-9 ? a/b : 0:\n  m_i  = safeDiv(M_i * qT_i(X_i),  Cn * qT_i(X_i) + M_c * qT_c(X_i))\n  a_i  = (M_i/Cn) * safeDiv(M_c * qT_c(X_c),  Cn * qT_i(X_c) + M_c * qT_c(X_c))   [accumulated for EVERY admitted i, including prev.W==0; skip only when canon is null (canon.wSum<=0), where w_c=0 makes it irrelevant]\n  m_c  = Σ_i a_i   (= 1 when k==0)\nPartition: per pair m_i + a_i = M_i/Cn identically for any nonnegative proxies; Σ budgets = 1. Support: m_i ∝ qT_i vanishes exactly off the neighbour's support; m_c ∝ qT_c(X_c) > 0 iff canon.targetPdf > 0.\nStreaming (WRS insert WITHOUT M increment; weights are fully normalized — no k factor anywhere):\n  neighbour i (only if prev_i.W>0 AND tp_i=pC(X_i)>0 AND m_i>0):  w_i = m_i * tp_i * prev_i.W   — the old *M_i factor is DELETED; winner stores targetPdf = tp_i.\n  canonical LAST: w_c = m_c * canon.targetPdf * canon.W.\nBookkeeping is unconditional per admitted source: k, Cn, a_i, and res.M += M_i happen on gate pass regardless of W/tp outcomes (the current early-returns at other.W<=0 and tp<=0 that skip M are the core of the measured +0.23/+0.32 and are removed).\nFinalize (outer only): res.W = safeDiv(res.wSum, res.targetPdf) — NO /M, NO /k. res.M = M_c + Cn (pure confidence for next frame's caps).\n\n=== GI ===\nReceivers: r_c = (h.p, h.n); r_i = (prev_i.visiblePos, pnd_i.xyz) — NO reconstruction; visiblePos is the exact point the neighbour's W is expressed for, and using it consistently at BOTH ratio points eliminates theory's refuted Error 2 (mismatched Jacobian reference points).\nProxy in the SHARED AREA MEASURE at x2, y = (sp, n2, rad): qG_r(y) = max(dot(n_r, w), 0) * luminance(rad) * abs(dot(n2, w)) / d2, w = normalize(sp−x_r), d2 = max(|sp−x_r|^2, 1e-6); 0 if degenerate. The |dot(n2,w)|/d2 factors ARE the solid-angle→area conversions, so NO Jacobian appears inside any ratio (verified: ratios of same-measure densities are measure-invariant). luminance(rad) is valid for a different outgoing direction only because materials are diffuse-only (Lo view-independent) — this leans on the project constraint.\nTrue target: pG(y) = giTarget(m, h.n, v, normalize(y.sp−h.p), y.rad).\n  m_i = safeDiv(M_i * qG_i(X_i),  Cn * qG_i(X_i) + M_c * qG_c(X_i))\n  a_i = (M_i/Cn) * safeDiv(M_c * qG_c(X_c),  Cn * qG_i(X_c) + M_c * qG_c(X_c)),  X_c = (canon.samplePos, canon.sampleNrm, canon.radiance)\nStreaming: w_i = m_i * pG(X_i) * prev_i.W * giJacobian(prev_i, h.p) — the existing clamped-[0,16] Jacobian multiplies the STREAM WEIGHT ONLY (it converts the neighbour's solid-angle UCW to ours; its clamp stays a documented residual). Guard |prev.samplePos−h.p|^2<=1e-6 → skip stream, keep accounting. Winner: visiblePos = h.p, targetPdf = pG(X_i). Canonical last: w_c = m_c * canon.targetPdf * W_c (identity shift, J=1). Finalize: res.W = min(safeDiv(wSum, targetPdf), 32) — cap kept, /M dropped (the ~1/M moved into the m's, so the cap's scale is comparable). res.M = M_c + Cn.\n\n=== CARRY (stored stream, DI and GI) ===\nThe mid-stream snapshot carry=res is INVALID under MIS (weights presume the full source set). Carry becomes its own exact two-source pair {canonical, temporal}, sharing all proxy/target/Jacobian evals with the big merge but using its own WRS draws:\n  m_t' = safeDiv(M_t*q_t(X_t), M_t*q_t(X_t) + M_c*q_c(X_t));  a_t' = safeDiv(M_c*q_c(X_c), M_t*q_t(X_c) + M_c*q_c(X_c))\n  w_t = m_t' * pTrue(X_t) * prev_t.W [* clamp(J,0,16) for GI];  w_c = a_t' * canon.targetPdf * W_c;  carry.M = M_c + M_t;  carry.W = safeDiv(wSum, targetPdf) [min(..,32) GI].\nNo temporal admitted → carry = canon under the OLD finalize. Spatial taps NEVER feed carry (the measured 1.28–1.93 feedback loop stays excluded).\n\n=== SHADING / VISIBILITY ===\nShadow ray on res's survivor; occluded → res.W=0, res.wSum=0 (exact for shading). The carry-transfer zeroing (pathtrace.wgsl:427-431 and 548-551) is REMOVED in the primary configuration: zeroing censors the stored stream's support in a visibility-correlated way next frame's proxies cannot see (Σm<1 on the gap → systematic temporal darkening; theory's analysis, confirmed by its verdict). Fallback if the A/B shows unacceptable variance/fireflies: re-enable and book a few-percent negative temporal bias.\n\n=== ACCEPTED RESIDUALS (documented, not fixed) ===\n(1) GI neighbour support over-coverage: GI fresh candidates are visible-by-construction from x_i, so the stored technique's support is visibility-limited while qG_i is not — bounded darkening at secondary-visibility boundaries (theory's Error 1; shared by every shipping ReSTIR GI without neighbour visibility rays). (2) GI J-clamp 16 and W-cap 32. (3) Current-frame light uniforms/flashmap inside proxies vs the prev frame the neighbour actually sampled — transient darkening only while lights move; invisible in the frozen-scene harness. (4) Sub-pixel jitter + rgba16float error in reconstructed x_i (DI) — thin boundary darkening, the safe under-coverage direction. (5) GI per-channel-albedo corner (monochromatic albedo orthogonal to radiance) — comment only.",
    "wgsl_plan": "== src/shaders/common.wgsl ==\n1. Uniforms struct (ends line ~187): append after _padRad2: `prevInvViewProj : mat4x4f, prevCamPos : vec3f, _padPrevCam : f32`.\n2. Add `fn safeDiv(a: f32, b: f32) -> f32 { return select(0.0, a/b, b > 1e-9); }`.\n3. Add `fn wrsAdd(r: ptr<function,Reservoir>, pos: vec3f, idx: u32, tp: f32, w: f32)` — reservoirUpdate (line 942) minus the M increment; keep reservoirUpdate itself for generateReservoir. GI needs NO new insert: giUpdate (line 1235) already takes `count` — call with count=0.0.\n4. Add `fn prevWorldFromPixel(qp: vec2i, dims: vec2u, dist: f32) -> vec3f`: uv=(vec2f(qp)+0.5)/vec2f(dims); ndc=(uv.x*2−1, 1−2*uv.y); h4 = U.prevInvViewProj * vec4f(ndc, 1.0, 1.0); return U.prevCamPos + normalize(h4.xyz/h4.w − U.prevCamPos) * dist. CRITICAL: ndc z = 1.0 to mirror cameraRay (common.wgsl:1151-1156) — this projection is REVERSE-Z INFINITE, z=0 unprojects to infinity and divides by ~0 (see camera.ts screenRay comment). pnd.w is the primary ray's Euclidean t (pathtrace.wgsl:599), so scaling the normalized direction by dist is exact up to sub-pixel jitter.\n5. Add `fn diProxyTarget(x: vec3f, nrm: vec3f, fv: f32, idx: u32, sp: vec3f) -> f32`: d=sp−x; d2=max(dot(d,d),1e-4); ndl=dot(nrm, d*inverseSqrt(d2)); if ndl<=0 return 0; return ndl * luminance(radianceFromLight(idx, x, sp)) * select(1.0, fv, idx==0u).\n6. Add `fn giProxyTarget(x: vec3f, nrm: vec3f, sp: vec3f, n2: vec3f, rad: vec3f) -> f32`: d=sp−x; d2=dot(d,d); if d2<=1e-6 return 0; w=d*inverseSqrt(d2); ndl=dot(nrm,w); if ndl<=0 return 0; return ndl * luminance(rad) * abs(dot(n2,w)) / d2.\n7. mergeReservoir (line 1006) and giMergePrev (pathtrace.wgsl:450): delete once the new loops land — the replacements live in pathtrace.wgsl because they need flashTargetVis and the two-phase context. finalizeReservoir/finalizeGIReservoir stay, used ONLY for the fresh canonical.\n\n== src/shaders/pathtrace.wgsl, restirDirect (lines 335-440) ==\n8. After generateReservoir: rename res→canon; call finalizeReservoir(&canon); precompute qcXc = select(0.0, diProxyTarget(h.p, h.n, fVis, canon.lightIdx, canon.samplePos), canon.wSum > 0.0).\n9. PHASE 1 (gather; the budget denominators need Cn before any m_i, so the streaming merge becomes two-phase): keep the existing reprojection + gate structure verbatim; on each gate pass record qp into `var nbQp: array<vec2i, 7>` and capped M into `var nbM: array<f32, 7>` (slot 0 = temporal; taps append; 1+6 max entries, ~21 scalars of private storage), accumulate Cn, count k. Only prev.M is read here (one storage load per source). rand2() tap offsets are drawn here exactly once, as today.\n10. If k==0 || Cn<=0: res = canon; carry = canon; skip to visibility (preserves the no-reuse path bit-for-bit).\n11. PHASE 2 (merge): var res = emptyReservoir(); var mC = 0.0; var carry-pair accumulators. For j in 0..k: reload pnd = textureLoad(prevNormalDepth, nbQp[j], 0) and prev = reservoirPrev[buf(nbQp[j])]; x_i = prevWorldFromPixel(nbQp[j], dims, pnd.w); n_i = pnd.xyz; fv_i = flashTargetVis(x_i) [this is why the loop lives in pathtrace.wgsl]; compute qS = diProxyTarget(x_i,n_i,fv_i, prev.lightIdx, prev.samplePos); qCXi = diProxyTarget(h.p,h.n,fVis, prev.lightIdx, prev.samplePos); tp_i = today's merge tp (risTarget + radianceFromLight at h.p + fVis select); qNXc = diProxyTarget(x_i,n_i,fv_i, canon.lightIdx, canon.samplePos); m_i = safeDiv(nbM[j]*qS, Cn*qS + canon.M*qCXi); if canon.wSum>0 { mC += (nbM[j]/Cn) * safeDiv(canon.M*qcXc, Cn*qNXc + canon.M*qcXc); } if prev.W>0 && tp_i>0 && m_i>0 { wrsAdd(&res, prev.samplePos, prev.lightIdx, tp_i, m_i*tp_i*prev.W); } — NO early return skips the accounting. If j==0 is temporal: also compute the k=1 carry pair from the SAME qS/qCXi/tp_i/qNXc/qcXc (m_t' = safeDiv(nbM0*qS, nbM0*qS + canon.M*qCXi); a_t' = safeDiv(canon.M*qcXc, nbM0*qNXc + canon.M*qcXc)) and wrsAdd into a separate carry reservoir.\n12. Canonical LAST: wrsAdd(&res, canon.samplePos, canon.lightIdx, canon.targetPdf, select(1.0, mC, k>0u) * canon.targetPdf * canon.W). res.M = canon.M + Cn; res.W = safeDiv(res.wSum, res.targetPdf). Carry: if temporal admitted, wrsAdd canonical with a_t'*canon.targetPdf*canon.W, carry.M = canon.M + nbM[0], carry.W = safeDiv(wSum, targetPdf); else carry = canon.\n13. Visibility block (415-436): keep res zeroing; DELETE the carry transfer-zeroing (427-431) — primary config; keep behind a const bool for the A/B, hardcode the winner after measurement. Store carry unchanged (438).\n\n== src/shaders/pathtrace.wgsl, restirGI (lines 480-559) ==\n14. Fresh candidate → canon (finalizeGIReservoir as today; canon.M = 1). qcXc = giProxyTarget(h.p, h.n, canon.samplePos, canon.sampleNrm, canon.radiance) when canon.wSum>0.\n15. Same two-phase gather (shares gate code shape; records qp + capped M; giMergePrev's W<=0/M<=0 early-return disappears — admission is geometry-only).\n16. Phase-2 per neighbour: prev = giPrev[buf]; pnd reload; receiver = (prev.visiblePos, pnd.xyz); qS = giProxyTarget(prev.visiblePos, pnd.xyz, prev.samplePos, prev.sampleNrm, prev.radiance); qCXi = giProxyTarget(h.p, h.n, prev.samplePos, prev.sampleNrm, prev.radiance); tp_i = giTarget(m, h.n, v, normalize(prev.samplePos−h.p), prev.radiance) with the d2<=1e-6 skip-stream guard; qNXc = giProxyTarget(prev.visiblePos, pnd.xyz, canon.samplePos, canon.sampleNrm, canon.radiance); J = giJacobian(prev, h.p) [existing, clamped]; m_i = safeDiv(M_i*qS, Cn*qS + 1.0*qCXi); mC += (M_i/Cn)*safeDiv(qcXc, Cn*qNXc + qcXc); stream via giUpdate(&res, prev.samplePos, prev.sampleNrm, prev.radiance, h.p, m_i*tp_i*prev.W*J, tp_i, 0.0) when prev.W>0 && tp_i>0 && J>0 && m_i>0. Temporal slot additionally feeds the carry pair (Jacobian included in its stream weight).\n17. Canonical last via giUpdate(count=0): weight select(1.0, mC, k>0u)*canon.targetPdf*canon.W. res.M = 1.0 + Cn; res.W = min(safeDiv(wSum, targetPdf), 32.0). Carry as DI. Delete carry-zeroing at 548-551 (same A/B toggle). Store carry.\n\n== notes ==\n- The winner's stored targetPdf is always the TRUE canonical target at the winner (finalize divides by it).\n- The same capped M_i variable feeds m_i, a_i, Cn, and res.M — one load, one variable.\n- RNG: phase-1 draws tap offsets, phase-2 draws WRS coins, carry uses its own coins (correlation with res is harmless — each stream is independently unbiased). The rand() consumption pattern changes vs today, so re-pin baselines in the same build.\n- Doc comments to update: restirDirect header (visibility carry-over claim), the carry rationale block (344-353), giMergePrev header, finalizeReservoir header (now inner-only).",
    "uniform_changes": "Two new uniforms, appended at the END of struct Uniforms (common.wgsl:37-187, after _padRad2 so every existing offset is untouched):\n  prevInvViewProj : mat4x4f   // inverse view-proj of the PREVIOUS frame (16-byte aligned; the radiosityOn+pads block ends 16-aligned)\n  prevCamPos      : vec3f     // camera position of the PREVIOUS frame\n  _padPrevCam     : f32\nTotal +80 bytes.\n\nTS plumbing:\n- src/game/camera.ts: add `prevInvViewProj: Mat4 = mat4Identity()` and `prevPos: Vec3 = v3(0,10,10)`; in update() (line 47, where prevViewProj is already retained) set `this.prevInvViewProj = this.invViewProj; this.prevPos = this.pos;` BEFORE rebuilding viewProj/invViewProj/pos. Note pos is rebuilt before the matrices in the current function body — capture prevPos at the top of update(), not next to line 47.\n- src/engine/renderer.ts FrameState (~line 277): add `prevInvViewProj: Mat4; prevCamPos: Vec3;`.\n- src/main.ts (~line 1253): pass `prevInvViewProj: camera.prevInvViewProj, prevCamPos: camera.prevPos` into the frame state.\n- src/engine/renderer.ts: UNIFORM_SIZE (line 26) += 80; in writeUniforms (line 1528) write `f.set(s.prevInvViewProj, BASE)` and prevCamPos at BASE+16..18, where BASE = first f32 index after the radiosityOn block (nominally f32 216 / byte 864 — recompute at edit time).\n- AUDIT WHILE THERE: the WGSL struct ends with puffParams (ends byte 848) + radiosityOn + 3 pads = 864 bytes, but UNIFORM_SIZE = 304 + DYN_GROUPS*16*2 + 32 + MAX_PUFFS*32 = 848 with DYN_GROUPS=8 — the 16-byte radiosityOn tail appears unaccounted (in-flight radiosity work in the dirty tree). Reconcile before appending, or the new mat4 lands 16 bytes short.\nNo new render settings; U.restirMCap and restirSpatialTaps keep their meanings (caps are now pure variance knobs). Reference mode already zeroes temporal/taps (renderer.ts:1560,1578), so the reference path needs no change.",
    "invariants": "1. Reuse disabled (taps=0 AND temporal off, or reference mode): k==0 path returns res = carry = canon under the OLD finalize — algebraically (and code-path) identical to the current fresh-RIS estimator; restirGI reduces to bounceWeight*rad exactly (the invariant its docstring already names). Check: __compareToReference {restirTemporal:false, restirSpatialTaps:0} matches the pre-change build within run-to-run noise.\n2. Temporal-only (k==1): the combine is the exact two-technique confidence-weighted balance heuristic m_t = M_t*q_t/(M_t*q_t + M_c*q_c) — no defensive dilution, full history weight; and carry's pair equals the shading pair (same formulas, kCarry=1).\n3. Partition of unity: per pair, m_i + a_i = M_i/Cn identically in the ratios (any nonnegative proxies), so Σ m_i + m_c = 1 pointwise wherever the canonical target is positive; a debug assertion can accumulate Σm−1 into a debugView channel.\n4. Equal-target reduction: if all proxies at a point are equal, m_i = M_i/(Cn+M_c) — confidence-proportional, matching the naive combine exactly where the naive combine was right.\n5. Outcome-independent bookkeeping: k, Cn, a_i accumulation, and res.M = M_c + Cn depend only on geometry gates and stored M — never on prev.W, wSum, or any target value. A neighbour with prev.W==0 changes nothing in them.\n6. One capped M_i everywhere: the same min(M,cap) value appears in m_i, a_i, Cn, and res.M.\n7. Finalize divisor is the winner's TRUE canonical targetPdf only (GI keeps the 32 cap); the old /(M*tp) finalize survives solely inside the fresh canonical.\n8. Stored stream (carry) is fed by fresh+temporal only — the spatial feedback loop (measured relBias 1.28–1.93) stays structurally excluded.\n9. m_i, a_i ∈ [0,1]; all streamed weights finite and >=0 via safeDiv guards; no division by anything that can underflow except through safeDiv.\n10. GI ratios contain NO Jacobian (area-measure proxies on both sides); the clamped giJacobian appears exactly once, in the neighbour's stream weight, with visiblePos as its reference — the same receiver the ratios use, so the pair is one consistent function (kills theory's Error 2).",
    "measurement_plan": "All runs from the browser console via window.__compareToReference (main.ts:984, defined at 705): frozen scene, forced-fresh history per config, reference = brute force (reuse auto-zeroed). Pin bounces in refOverrides (its own docs: pin anything that changes transport). Canonical call, identical for every build:\n\nawait __compareToReference({\n  freshOnly:    { restirTemporal: false, restirSpatialTaps: 0 },\n  temporalOnly: { restirTemporal: true,  restirSpatialTaps: 0 },\n  spatialOnly:  { restirTemporal: false, restirSpatialTaps: 6 },\n  combined:     { restirTemporal: true,  restirSpatialTaps: 6 },\n}, 400, 120, { bounces: 2 })\n\nThree builds: (A) pre-change HEAD — re-pin baselines in-tree (RNG consumption changes, so stale numbers are not comparable); expected ≈ temporal +0.23, spatial −0.02..−0.05 / relRmse 0.65–0.74, combined +0.32 / 1.08. (B) MIS with carry-zeroing KEPT (isolates the merge fix). (C) MIS with carry-zeroing REMOVED (primary spec).\n\nACCEPT the change iff, in build C:\n- freshOnly: relBias and relRmse match build A within run noise (~±0.01) — invariant 1 regression gate; any drift is a plumbing bug, stop and fix.\n- temporalOnly: |relBias| <= 0.03 (build B may sit slightly NEGATIVE, up to ~−0.05 — that is the zeroing residual and is expected).\n- spatialOnly: |relBias| <= 0.03 and relRmse <= 0.74 (no regression).\n- combined: |relBias| <= 0.05 AND combined relRmse < spatialOnly relRmse in the same run — the headline criterion (combining must win). Target band 0.45–0.65.\n- maxRelErr in C not materially worse than B (firefly gate on the zeroing removal). Choose C if it passes; else ship B and document the small negative temporal bias.\n\nREJECT / debug paths: combined bias ~0 but relRmse >= spatialOnly → suspect Cn/k miscount (a W==0 neighbour dropped from the accounting, or a rejected tap counted) before touching the math; residual positive bias → an early-return still skipping M/a_i accumulation; residual negative bias beyond band → attribute GI by re-running the same four configs with restirGI:false, then with restirGI:true — GI carries the documented negative residuals (J-clamp 16, W-cap 32, neighbour-visibility support over-coverage), DI should be clean to ±0.03; if DI is clean and GI exceeds ~−0.05, either accept-and-document or prototype the neighbour visibility-ray fix for the a_i terms and re-measure.\nLight-motion caveat: the frozen-scene harness cannot see the current-frame-target drift; verify visually (flashlight sweep, guard patrol) for transient darkening trails.\nPer MEMORY.md, also measure perf and pixels: run the existing bench(90) on A and C at the pinned pose (expect ~1–3% from 2 proxy evals + unproject + flashmap tap per admitted neighbour; watch for register-spill cliffs from the two-phase arrays) and screenshot-diff a lit interior + a penumbra region before calling it done.",
    "open_risks": "1. Combined-mode RMSE is the goal but not a theorem: even unbiased, 6 spatial taps + temporal may not beat spatial-only. The confidence budgets (temporal keeps up to M_t/Cn ≈ 20/50 of the neighbour mass, vs 1/7 under uniform pairwise) are the mitigation; remaining levers are the caps (now bias-free variance knobs), tap count, or full O(k^2) Talbot MIS.\n2. The hybrid itself (theory's confidence budgets + code's proxy-on-both-sides ratios) was not adversarially verified as a unit — each half was, and the partition identity is checked per pair in invariant 3 — but the debug assertion (Σm−1) should be run once before trusting measurements.\n3. GI neighbour-visibility over-coverage (theory's refuted Error 1) is a structural residual of ALL three derivations, not fixed here: fresh GI candidates are visible-by-construction from their receiver, the proxy cannot test visibility, so expect bounded darkening at secondary-visibility boundaries; the GI acceptance band is widened to −0.05 for this. Fix if needed: one x_i→X_c visibility ray per admitted neighbour inside a_i (k extra rays/pixel).\n4. Removing the carry-zeroing trades away the \"don't hand a known-shadowed sample to next frame\" variance saving the restirDirect docstring credits as its main win; the A/B decides, and re-enabling costs a few percent negative temporal bias, not correctness of shading.\n5. Register pressure: two-phase gather adds ~21 scalars (qp+M per source) plus phase-2 temporaries to an already-heavy kernel; a spill would show as a perf cliff in bench() — measure, and shrink the arrays (pack qp into one u32) if hit.\n6. Uniform-size bookkeeping: the current UNIFORM_SIZE formula appears 16 bytes short of the WGSL struct (radiosityOn tail, in-flight radiosity work in the dirty tree, tasks #9-#11) — appending the new mat4 without reconciling will misalign it; coordinate with that work before editing.\n7. Temporal target drift (proxies evaluate current-frame flashlight/torch uniforms and the current flashmap at prev-frame receivers) is invisible to the frozen-scene harness and appears as transient darkening while lights move — pre-existing, industry-standard, but unmeasured here; check visually.\n8. DI receiver reconstruction error (pixel-center ray vs the jittered ray that measured pnd.w, rgba16float precision) gives cm-scale x_i error entering only the proxies — safe under-coverage direction, but concentrated at spot-cone edges and terminators; if spatialOnly bias drifts negative beyond band, tighten here first (e.g. store a dedicated prev-position, or accept).\n9. First frames / disocclusion: prevInvViewProj starts as identity and prevNormalDepth is zeroed; the geometry gates reject those sources for h.t > ~0.14, and compareToReference force-resets history per config, but very-near-camera surfaces in frame 1 could admit garbage receivers — the M_i=0/proxy-zero conventions make this variance, not bias.\n10. Deleting mergeReservoir/giMergePrev changes common.wgsl shared by other passes — grep for other call sites before removal (only restirDirect/restirGI call them today, but the tree is dirty with parallel work)."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Derive"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "derive:theory",
      "phaseIndex": 1,
      "phaseTitle": "Derive",
      "agentId": "a9e6a3f888584e16a",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381168081,
      "lastProgressAt": 1785381168081,
      "cached": true,
      "resultPreview": "{\"di_formulas\":\"SOURCES per pixel: canonical = fresh reservoir from generateReservoir (unchanged internally; its own finalize wSum/(M*targetPdf) stays valid because candidates are uniform over ALL lights, i.e. constant MIS 1/M with full support). Confidence c_c = can.M (~8). Neighbours j=1..k = {temporal at pp if restirTemporal && compat pass && prev.M>0} ∪ {each spatial tap at qp passing bounds+d…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "derive:recipe",
      "phaseIndex": 1,
      "phaseTitle": "Derive",
      "agentId": "a8630b2efe22ee0a0",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381168081,
      "lastProgressAt": 1785381168081,
      "cached": true,
      "resultPreview": "{\"di_formulas\":\"SETUP (applies to DI and GI): the combine is restructured into (a) the UNCHANGED inner fresh RIS (generateReservoir, weights tp*total, old finalize W_c = wSum/(M_c*targetPdf)) producing the CANONICAL reservoir `canon`, and (b) an OUTER pairwise-MIS combine over k+1 techniques = {canon} + k admitted neighbours. k MUST be fixed before any streaming, so a gather pass runs the existing…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "derive:code",
      "phaseIndex": 1,
      "phaseTitle": "Derive",
      "agentId": "a5908f170c221047e",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381168081,
      "lastProgressAt": 1785381168081,
      "cached": true,
      "resultPreview": "{\"di_formulas\":\"SETUP. Per pixel, the combine is one canonical technique plus k neighbour techniques, where k counts every neighbour that passes the GEOMETRY gates only (reprojection on-screen, clip.w>0, depth/normal compat). Gates may not depend on reservoir contents.\\n\\n- Canonical c: R_c = generateReservoir(...) finalized with the OLD formula W_c = wSum/(M_c * targetPdf). This stays as-is: insi…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "refute-a:theory",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a39def4b2a50f025d",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381171651,
      "queuedAt": 1785381168103,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "VERIFIED SOUND (most of the derivation): (1) Pairwise parti…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…",
      "lastProgressAt": 1785381698002,
      "tokens": 79301,
      "toolCalls": 5,
      "durationMs": 526351,
      "resultPreview": "{\"refuted\":true,\"findings\":\"VERIFIED SOUND (most of the derivation): (1) Pairwise partition of unity is exact: m_j + alpha_j = (c_j*q_j + (c_j*c_c/Cn)*p_c)/(Cn*q_j + c_c*p_c) = c_j/Cn identically, so Sum_j m_j + m_c = 1 wherever any pair denominator is positive; on {p_c > 0} every denominator >= c_c*p_c > 0, and points with p_c = 0 are annihilated by the w = m*p_c*W factor, so restricting conditio…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "refute-b:theory",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a7bc743c071ec6d85",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381172250,
      "queuedAt": 1785381168103,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SURVIVES SCRUTINY. Math checks: (1) Partition of unity veri…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…",
      "lastProgressAt": 1785381537403,
      "tokens": 67541,
      "toolCalls": 7,
      "durationMs": 365153,
      "resultPreview": "{\"refuted\":false,\"findings\":\"SURVIVES SCRUTINY. Math checks: (1) Partition of unity verified algebraically — per pair, m_j(y) + alpha_j(y) = (c_j*q_j + (c_j*c_c/Cn)*p_c) / (Cn*q_j + c_c*p_c) = c_j/Cn exactly, summing to 1 over j; holds for ANY positive q_j, so the albedo-less proxy is legal as claimed. (2) GI Jacobian placement rederived from first principles in the area measure at x2: neighbour d…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "refute-a:recipe",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aaa90841be9827329",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381171138,
      "queuedAt": 1785381168109,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Adversarial verification failed to find a bias-producing er…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…",
      "lastProgressAt": 1785381612779,
      "tokens": 80609,
      "toolCalls": 6,
      "durationMs": 441641,
      "resultPreview": "{\"refuted\":false,\"findings\":\"Adversarial verification failed to find a bias-producing error or broken estimator. Checks performed against both the math and the actual shaders (/Users/sujayakar/src/twopointfive/src/shaders/common.wgsl, /Users/sujayakar/src/twopointfive/src/shaders/pathtrace.wgsl):\\n\\n1. PARTITION OF UNITY: exact. m_c + SUM m_i = (1/(k+1))[1 + SUM(1-alpha_i) + SUM alpha_i] = 1 teles…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "refute-b:recipe",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a62ce0f8387a7751f",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381173110,
      "queuedAt": 1785381168109,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "No bias-producing or estimator-breaking error found; the de…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…",
      "lastProgressAt": 1785381572850,
      "tokens": 95077,
      "toolCalls": 10,
      "durationMs": 399740,
      "resultPreview": "{\"refuted\":false,\"findings\":\"No bias-producing or estimator-breaking error found; the derivation survives both the math and the practicality check.\\n\\nVERIFIED CORRECT: (1) Partition of unity is exact by construction — sum m = (1/(k+1))[sum(alpha_i) + 1 + sum(1-alpha_i)] = 1 pointwise for ANY alpha functions, so proxy quality affects variance only, matching the GRIS theorem's actual conditions. (2…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "refute-a:code",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a3710f063ecb3b23d",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381170895,
      "queuedAt": 1785381168114,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I attempted to break the derivation on every axis requested…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…",
      "lastProgressAt": 1785381563670,
      "tokens": 39457,
      "toolCalls": 1,
      "durationMs": 392774,
      "resultPreview": "{\"refuted\":false,\"findings\":\"I attempted to break the derivation on every axis requested and could not find an error that produces bias or a broken estimator. Checks performed: (1) Partition of unity: per pair, mu_i + pi_i = M_i*q_i/D + M_c*q_c/D = 1 pointwise for every realization of the (capped) M's, and the 1/k average over k pairs sums the full technique set to exactly 1; the degenerate conven…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "refute-b:code",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab6513406f40a917b",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381174012,
      "queuedAt": 1785381168114,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Survives scrutiny; no bias-producing or estimator-breaking …",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…",
      "lastProgressAt": 1785381664995,
      "tokens": 89006,
      "toolCalls": 14,
      "durationMs": 490983,
      "resultPreview": "{\"refuted\":false,\"findings\":\"Survives scrutiny; no bias-producing or estimator-breaking error found. Verified against the actual shaders: (1) the confidence-weighted pairwise weights are an exact pointwise partition of unity, and the k-scaled streaming + divide-by-max(k,1) finalize is selection-invariant and correct; (2) charging k/pi_i/M_i for null and per-sample-degenerate neighbours while gatin…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "synthesize",
      "phaseIndex": 3,
      "phaseTitle": "Synthesize",
      "agentId": "a3e0dff92e26e5f39",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1785381702397,
      "queuedAt": 1785381699885,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "FINAL SCHEME: confidence-budget pairwise MIS (canonical anc…",
      "promptPreview": "You are working on a WebGPU real-time path tracer (2.5D stealth demo, diffuse-only, axis-aligned boxes). Its ReSTIR DI and GI merges currently use the naive M-proportional combine, which is biased. Here is the EXACT current scheme:\n\nReservoir struct: { samplePos: vec3f, lightIdx: u32, wSum: f32, M: f32, W: f32, targetPdf: f32 }\n\nfn reservoirUpdate(r, pos, idx, targetPdf, w) { r.wSum += w; r.M += 1…",
      "lastProgressAt": 1785382317220,
      "tokens": 119606,
      "toolCalls": 20,
      "durationMs": 614822,
      "resultPreview": "{\"spec\":\"FINAL SCHEME: confidence-budget pairwise MIS (canonical anchored in every pair), with albedo-less PROXY targets on BOTH sides of every MIS ratio and the TRUE canonical target reserved for resampling weights, stored targetPdf, and finalize. Backbone is the \\\"code\\\" derivation (survived both verdicts, verified against the shaders); pair budgets M_i/Cn come from the \\\"theory\\\" derivation (it…"
    }
  ],
  "totalTokens": 570597,
  "totalToolCalls": 63
}